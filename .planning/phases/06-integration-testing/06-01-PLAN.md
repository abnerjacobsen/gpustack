---
phase: 06-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/cloud_providers/test_aws.py
autonomous: true

must_haves:
  truths:
    - "Factory can instantiate AWSClient from CloudCredential with AWS provider type"
    - "Factory correctly extracts access_key, secret_key, and region from CloudCredential"
    - "Factory passes options (vpc_id, subnet_id, security_group_id) to AWSClient config"
    - "get_client_from_provider() raises ValueError for unsupported providers"
  artifacts:
    - path: "tests/cloud_providers/test_aws.py"
      provides: "Factory integration tests for get_client_from_provider()"
      min_lines: 80
  key_links:
    - from: "tests/cloud_providers/test_aws.py"
      to: "gpustack/cloud_providers/common.py"
      via: "import get_client_from_provider"
      pattern: "from gpustack.cloud_providers.common import get_client_from_provider"
    - from: "get_client_from_provider(ClusterProvider.AWS, credential)"
      to: "AWSClient instance"
      via: "factory lambda execution"
      pattern: "assert isinstance.*AWSClient"
---

<objective>
Create comprehensive factory integration tests to verify the provider factory correctly instantiates AWSClient with proper credential extraction from CloudCredential.

Purpose: The factory registration exists in common.py but lacks dedicated tests for the credential extraction lambda. This plan ensures the factory correctly maps CloudCredential fields to AWSClient parameters.

Output: Factory integration tests in tests/cloud_providers/test_aws.py covering all credential extraction scenarios.
</objective>

<execution_context>
@/home/abner/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/abner/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@gpustack/cloud_providers/common.py
@gpustack/schemas/clusters.py

# Factory registration is already implemented in common.py lines 21-46:
# factory: Dict[ClusterProvider, Tuple[Type[ProviderClientBase], Callable[[CloudCredential], ProviderClientBase]]] = {
#     ClusterProvider.AWS: (
#         AWSClient,
#         lambda credential: AWSClient(
#             access_key=credential.key or "",
#             secret_key=credential.secret or "",
#             region=credential.options.get("region", "us-east-1") if credential.options else "us-east-1",
#             config=AWSConfig(...),
#         ),
#     ),
# }
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create factory integration tests for get_client_from_provider()</name>
  <files>tests/cloud_providers/test_aws.py</files>
  <action>
Add comprehensive factory integration tests to tests/cloud_providers/test_aws.py after the EBS volume tests (around line 1600). Include these test functions:

1. `test_get_client_from_provider_aws_success()` - Test successful instantiation:
   - Create CloudCredential with ClusterProvider.AWS
   - Set key="AKIAIOSFODNN7EXAMPLE", secret="wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
   - Set options={"region": "us-west-2", "vpc_id": "vpc-12345", "subnet_id": "subnet-67890", "security_group_id": "sg-11111"}
   - Call get_client_from_provider(ClusterProvider.AWS, credential)
   - Assert result is AWSClient instance
   - Assert result.access_key == credential.key
   - Assert result.region == "us-west-2"
   - Assert result.config.vpc_id == "vpc-12345"

2. `test_get_client_from_provider_aws_default_region()` - Test default region extraction:
   - Create CloudCredential with options=None or empty dict
   - Call get_client_from_provider(ClusterProvider.AWS, credential)
   - Assert result.region == "us-east-1" (default)

3. `test_get_client_from_provider_aws_partial_options()` - Test partial options:
   - Create CloudCredential with options={"region": "eu-west-1", "subnet_id": "subnet-test"}
   - No vpc_id or security_group_id
   - Assert AWSClient created successfully with subnet_id set, vpc_id=None

4. `test_get_client_from_provider_aws_no_options()` - Test credential without options:
   - Create CloudCredential with options=None
   - Assert AWSClient created with region="us-east-1" and config=None

5. `test_get_client_from_provider_unsupported_provider()` - Test error handling:
   - Try get_client_from_provider with unsupported provider (if possible) or mock
   - Assert ValueError raised with message "Unsupported provider"

Use pytest.mark.asyncio for any async test helpers if needed, but factory tests themselves are synchronous. Import get_client_from_provider from gpustack.cloud_providers.common.
  </action>
  <verify>Run `python -m pytest tests/cloud_providers/test_aws.py::test_get_client_from_provider* -v` - all 5 tests pass</verify>
  <done>5 factory integration tests exist and pass, verifying INTG-02 requirement</done>
</task>

<task type="auto">
  <name>Task 2: Add test for factory lambda credential extraction edge cases</name>
  <files>tests/cloud_providers/test_aws.py</files>
  <action>
Add additional factory tests for edge cases:

1. `test_factory_lambda_empty_strings()` - Test empty key/secret handling:
   - Create CloudCredential with key="", secret=""
   - Assert AWSClient created (implementation allows empty strings, validation happens later)

2. `test_factory_lambda_special_characters_in_secret()` - Test secret with special chars:
   - Create CloudCredential with secret containing special characters
   - Assert AWSClient created correctly

3. `test_factory_lambda_long_region_name()` - Test region format validation:
   - Create CloudCredential with options={"region": "very-long-region-name-that-is-invalid"}
   - Assert AWSClient created (format validation happens in AWSConfig/AWSClient, not factory)

Ensure tests verify the factory correctly extracts and passes ALL credential fields to AWSClient constructor.
  </action>
  <verify>Run `python -m pytest tests/cloud_providers/test_aws.py::test_factory_lambda* -v` - all 3 tests pass</verify>
  <done>3 additional factory edge case tests pass, verifying robust credential extraction</done>
</task>

</tasks>

<verification>
- All 8 factory integration tests pass
- Tests verify INTG-02 requirement: Provider factory registers AWSClient for AWS provider type
- Tests cover credential extraction, default values, and error handling
- No modifications to source code (factory already implemented)
</verification>

<success_criteria>
- Factory integration tests exist and pass (8 total)
- Tests verify CloudCredential → AWSClient mapping works correctly
- All credential fields (key, secret, region, options) correctly extracted and passed
- Error handling for unsupported providers tested
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-testing/06-01-SUMMARY.md`
</output>
