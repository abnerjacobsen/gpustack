---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gpustack/schemas/clusters.py
  - gpustack/schemas/aws.py
autonomous: true
must_haves:
  truths:
    - ClusterProvider enum includes AWS as a provider option
    - AWS configuration schema validates required fields (access key, secret key, region)
    - AWS configuration schema supports optional fields (VPC, subnet, security group)
    - Type hints and Pydantic validators are properly defined
  artifacts:
    - path: gpustack/schemas/clusters.py
      provides: ClusterProvider.AWS enum member
      contains: 'AWS = "AWS"'
    - path: gpustack/schemas/aws.py
      provides: AWSConfig schema with validation
      contains:
        - "access_key"
        - "secret_key"
        - "region"
        - "vpc_id"
        - "subnet_id"
        - "security_group_id"
  key_links:
    - from: gpustack/schemas/aws.py
      to: gpustack/schemas/clusters.py
      via: ClusterProvider enum usage
---

<objective>
Create AWS configuration schema and add AWS to ClusterProvider enum. This establishes the type foundation that all subsequent AWS provider work depends on.

Purpose: Provide the data structures and type definitions required for AWS cloud provider integration.
Output: Updated ClusterProvider enum with AWS, new AWSConfig schema file.
</objective>

<execution_context>
@/home/abner/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@gpustack/schemas/clusters.py

# Reference: DigitalOcean pattern for credential structure
@gpustack/cloud_providers/digital_ocean.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AWS to ClusterProvider enum</name>
  <files>gpustack/schemas/clusters.py</files>
  <action>
    Add AWS to the ClusterProvider enum in clusters.py.
    
    Locate the ClusterProvider enum (around line 154-157) and add:
    AWS = "AWS"
    
    Ensure proper enum formatting following existing pattern:
    ```python
    class ClusterProvider(Enum):
        Docker = "Docker"
        Kubernetes = "Kubernetes"
        DigitalOcean = "DigitalOcean"
        AWS = "AWS"
    ```
    
    The enum value "AWS" matches the pattern where DigitalOcean = "DigitalOcean".
  </action>
  <verify>grep -n 'AWS = "AWS"' gpustack/schemas/clusters.py</verify>
  <done>ClusterProvider enum contains AWS = "AWS" member</done>
</task>

<task type="auto">
  <name>Task 2: Create AWSConfig schema</name>
  <files>gpustack/schemas/aws.py</files>
  <action>
    Create gpustack/schemas/aws.py with AWS configuration schema.
    
    Create AWSConfig Pydantic model with:
    - access_key (str, required): AWS access key ID
    - secret_key (str, required): AWS secret access key (sensitive)
    - region (str, required): AWS region (e.g., us-east-1)
    - vpc_id (str, optional): VPC ID for instance placement
    - subnet_id (str, optional): Subnet ID for instance placement
    - security_group_id (str, optional): Security group ID
    
    Requirements:
    - Use Pydantic BaseModel with proper type hints
    - Add field validators for region format (lowercase with hyphens)
    - Add validator for access_key format (alphanumeric, typical AKIA pattern)
    - Mark secret_key as sensitive (use SecretStr or similar pattern)
    - Include ConfigDict(extra="ignore") for forward compatibility
    
    Reference DigitalOcean pattern where secret is handled sensitively.
  </action>
  <verify>python -c "from gpustack.schemas.aws import AWSConfig; print('OK')"</verify>
  <done>AWSConfig schema file exists with all required fields and validators</done>
</task>

<task type="auto">
  <name>Task 3: Update CloudCredential options type support</name>
  <files>gpustack/schemas/clusters.py</files>
  <action>
    Update CloudCredentialBase to properly support AWS provider options.
    
    The CloudCredentialBase already has an 'options' field (Dict[str, Any]) that can store 
    provider-specific settings. No changes needed to the base schema - AWS-specific 
    options (vpc_id, subnet_id, security_group_id) will be stored in this JSON field.
    
    Just verify the existing schema works for AWS:
    - provider field can be ClusterProvider.AWS
    - key field can store access_key
    - secret field (in CloudCredentialUpdate) stores secret_key
    - options dict stores vpc_id, subnet_id, security_group_id
    
    Document this mapping in comments for clarity.
  </action>
  <verify>python -c "from gpustack.schemas.clusters import CloudCredential, ClusterProvider; c = CloudCredential(provider=ClusterProvider.AWS, name='test', key='AKIA...'); print('OK')"</verify>
  <done>CloudCredential schema confirmed compatible with AWS provider requirements</done>
</task>

</tasks>

<verification>
1. ClusterProvider.AWS exists in enum
2. AWSConfig schema imports successfully with all fields
3. Region validator rejects invalid formats
4. Type checking passes: mypy gpustack/schemas/aws.py
</verification>

<success_criteria>
- ClusterProvider enum includes AWS = "AWS"
- gpustack/schemas/aws.py exists with AWSConfig class
- AWSConfig validates required fields (access_key, secret_key, region)
- AWSConfig accepts optional fields (vpc_id, subnet_id, security_group_id)
- All type hints pass mypy checking
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
