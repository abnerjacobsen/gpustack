---
phase: 02-ssh-key-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gpustack/cloud_providers/aws.py
autonomous: true

must_haves:
  truths:
    - "System can import SSH public key to AWS EC2"
    - "Key pair name is generated with gpustack-{worker-name}-{suffix} pattern"
    - "System checks for key existence before import to prevent duplicates"
    - "System returns AWS key pair name for instance creation"
    - "AWS key pairs are tagged with ManagedBy=GPUStack"
  artifacts:
    - path: "gpustack/cloud_providers/aws.py"
      provides: "create_ssh_key method implementation"
      contains: "async def create_ssh_key"
    - path: "gpustack/cloud_providers/aws.py"
      provides: "Key name generation with random suffix"
      contains: "secrets.token_hex"
    - path: "gpustack/cloud_providers/aws.py"
      provides: "Key existence check helper"
      contains: "describe_key_pairs"
  key_links:
    - from: "create_ssh_key"
      to: "import_key_pair API"
      via: "aiobotocore EC2 client"
      pattern: "await client.import_key_pair"
    - from: "create_ssh_key"
      to: "TagSpecifications"
      via: "AWS tagging for resource management"
      pattern: "ResourceType.*key-pair"
---

<objective>
Implement AWS EC2 SSH key pair creation via import_key_pair API with proper naming, collision detection, and tagging.

Purpose: Enable GPUStack to create SSH key pairs in AWS EC2 for GPU instance access, following the ProviderClientBase interface pattern.
Output: Working create_ssh_key method with collision prevention and AWS tagging.
</objective>

<execution_context>
@/home/abner/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/abner/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Reference Implementation
@gpustack/cloud_providers/aws.py - AWSClient class with create_ssh_key stub (lines 240-255)
@gpustack/cloud_providers/digital_ocean.py - create_ssh_key reference pattern (lines 124-129)
@gpustack/cloud_providers/abstract.py - ProviderClientBase interface definition (line 75)

## Research
@.planning/phases/02-ssh-key-management/02-RESEARCH.md - ED25519 key format, AWS import_key_pair patterns
@.planning/phases/02-ssh-key-management/02-CONTEXT.md - Key naming decisions, collision handling

## Phase 1 Foundation
@.planning/phases/01-foundation/01-03-SUMMARY.md - Factory integration complete, validate_credentials working
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement create_ssh_key method with import_key_pair</name>
  <files>gpustack/cloud_providers/aws.py</files>
  <action>
Replace the NotImplementedError stub in create_ssh_key method (lines 240-255) with full implementation:

1. Generate key pair name using pattern: `gpustack-{worker_name}-{suffix}` where suffix is 8-char hex from secrets.token_hex(4)

2. Import secrets module at top of file if not present

3. Implement the method:
   - Generate key_name with pattern above
   - Use async with self._get_client() as client
   - Call describe_key_pairs(KeyNames=[key_name]) first to check existence
   - If key exists (no ClientError), raise RuntimeError with clear message including fingerprint if available
   - If ClientError with InvalidKeyPair.NotFound, proceed with import
   - Call import_key_pair with:
     - KeyName=key_name
     - PublicKeyMaterial=public_key.encode('utf-8')
     - TagSpecifications with ResourceType='key-pair' and Tags: ManagedBy=GPUStack, WorkerName={worker_name}
   - Log success with fingerprint from response
   - Return response['KeyName']

4. Handle specific error codes:
   - InvalidKey.Format: raise RuntimeError with helpful message about OpenSSH format
   - InvalidKeyPair.Duplicate: raise RuntimeError (shouldn't happen if check worked, but handle defensively)
   - Other ClientError: use self._handle_aws_error(e, "SSH key import")

5. Follow the exact pattern from RESEARCH.md section "AWS Import Key Pair with Error Handling"

IMPORTANT: Do NOT modify delete_ssh_key yet - that is Plan 02.
  </action>
  <verify>
grep -n "async def create_ssh_key" gpustack/cloud_providers/aws.py | head -1
grep -n "import_key_pair" gpustack/cloud_providers/aws.py
grep -n "secrets.token_hex" gpustack/cloud_providers/aws.py
grep -n "TagSpecifications" gpustack/cloud_providers/aws.py
grep -n "ManagedBy.*GPUStack" gpustack/cloud_providers/aws.py
  </verify>
  <done>
create_ssh_key method is fully implemented (not raising NotImplementedError), uses import_key_pair API, includes collision detection with describe_key_pairs, generates names with gpustack-{worker_name}-{suffix} pattern, and applies AWS tags.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add helper method for key existence check</name>
  <files>gpustack/cloud_providers/aws.py</files>
  <action>
Add a private helper method _check_key_exists after the _handle_aws_error method (around line 121) and before validate_credentials:

```python
async def _check_key_exists(self, client, key_name: str) -> tuple[bool, Optional[str]]:
    """Check if EC2 key pair exists in AWS.
    
    Args:
        client: aiobotocore EC2 client
        key_name: Name of the key pair to check
        
    Returns:
        Tuple of (exists: bool, fingerprint: Optional[str])
    """
    try:
        response = await client.describe_key_pairs(KeyNames=[key_name])
        key_info = response.get('KeyPairs', [{}])[0]
        fingerprint = key_info.get('KeyFingerprint')
        return True, fingerprint
    except ClientError as e:
        if e.response['Error']['Code'] == 'InvalidKeyPair.NotFound':
            return False, None
        raise self._handle_aws_error(e, "SSH key existence check")
```

Then update create_ssh_key to use this helper method instead of inline try/except for the existence check.

Add Optional to the imports if not already present: `from typing import Optional, List, Dict, Any, Tuple`

This follows the "Check Before Create" pattern from RESEARCH.md and makes the code more testable.
  </action>
  <verify>
grep -n "_check_key_exists" gpustack/cloud_providers/aws.py
grep -n "describe_key_pairs" gpustack/cloud_providers/aws.py
  </verify>
  <done>
_check_key_exists helper method exists, returns tuple[bool, Optional[str]], and is used by create_ssh_key for collision detection before import.
  </done>
</task>

</tasks>

<verification>
1. Code structure verification:
   - grep "async def create_ssh_key" - method exists and is async
   - grep "import_key_pair" - uses AWS import API
   - grep "_check_key_exists" - helper method exists
   - grep "TagSpecifications" - AWS tagging present

2. Error handling verification:
   - InvalidKeyPair.NotFound handled in _check_key_exists
   - InvalidKey.Format handled in create_ssh_key
   - InvalidKeyPair.Duplicate handled in create_ssh_key

3. Naming pattern verification:
   - grep "secrets.token_hex(4)" - 8-char hex suffix
   - Key name format: "gpustack-{worker_name}-{suffix}"
</verification>

<success_criteria>
1. create_ssh_key no longer raises NotImplementedError
2. Method uses aiobotocore EC2 client import_key_pair API
3. Key names follow gpustack-{worker_name}-{suffix} pattern with 8-char hex
4. Existence check with describe_key_pairs prevents duplicate key errors
5. AWS tags applied: ManagedBy=GPUStack, WorkerName={worker_name}
6. Error handling covers InvalidKey.Format and InvalidKeyPair.Duplicate
7. Returns AWS KeyName (same as generated name)
</success_criteria>

<output>
After completion, create `.planning/phases/02-ssh-key-management/02-01-SUMMARY.md`
</output>
