---
phase: 04-instance-lifecycle-waiting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gpustack/cloud_providers/aws.py
  - tests/cloud_providers/test_aws.py
autonomous: true
must_haves:
  truths:
    - System polls instance status until it reaches RUNNING state
    - wait_for_started() returns CloudInstance when instance is running
    - wait_for_started() raises TimeoutError if limit exceeded
    - wait_for_started() handles InvalidInstanceID.NotFound with retry logic
    - Polling uses exponential backoff (not fixed interval)
  artifacts:
    - path: gpustack/cloud_providers/aws.py
      provides: wait_for_started() method
      exports: ["wait_for_started"]
      min_lines: 30
    - path: tests/cloud_providers/test_aws.py
      provides: Test coverage for wait_for_started()
      contains: ["test_wait_for_started_success", "test_wait_for_started_timeout", "test_wait_for_started_not_found_retry"]
  key_links:
    - from: wait_for_started()
      to: get_instance()
      via: instance = await self.get_instance(external_id)
      pattern: "get_instance.*external_id"
    - from: wait_for_started()
      to: status_mapping
      via: status_mapping.get(aws_state) == InstanceState.RUNNING
      pattern: "status == InstanceState.RUNNING"
---

<objective>
Implement wait_for_started() method to poll EC2 instance until it reaches RUNNING state.

Purpose: GPUStack workers need to be fully running before they can be configured and connected. This method provides reliable polling with exponential backoff and handles AWS's eventual consistency.
Output: Working wait_for_started() method with comprehensive tests.
</objective>

<execution_context>
@/home/abner/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/abner/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@gpustack/cloud_providers/aws.py
@gpustack/cloud_providers/digital_ocean.py
@tests/cloud_providers/test_aws.py
@gpustack/cloud_providers/abstract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement wait_for_started() method</name>
  <files>gpustack/cloud_providers/aws.py</files>
  <action>
    Replace the NotImplementedError stub for wait_for_started() with full implementation.

    Implementation requirements:
    1. Poll get_instance() in a loop until instance.status == InstanceState.RUNNING
    2. Use exponential backoff: sleep_time = backoff * (2 ** attempt) - capped at 60s max
    3. Handle InvalidInstanceID.NotFound by continuing to retry (AWS eventual consistency)
    4. Raise TimeoutError with descriptive message if limit exceeded
    5. Return CloudInstance once running

    Key details:
    - Keep the method signature: async def wait_for_started(self, external_id: str, backoff: int = 15, limit: int = 40)
    - Log each poll attempt at DEBUG level: logger.debug(f"Waiting for instance {external_id}, attempt {attempt}/{limit}, status: {status}")
    - After loop, check if instance is None and raise RuntimeError (not expected after NotFound handling)
    - Follow DigitalOceanClient pattern but add exponential backoff

    Exponential backoff calculation:
    ```python
    sleep_time = min(backoff * (2 ** attempt), 60)  # Cap at 60 seconds
    ```

    InvalidInstanceID.NotFound handling:
    - When get_instance() returns None (from NotFound error), treat as instance not yet visible
    - Continue polling - AWS eventual consistency means instance may not appear immediately
    - Log at DEBUG: "Instance {external_id} not yet visible, retrying..."
  </action>
  <verify>
    grep -A 50 "async def wait_for_started" gpustack/cloud_providers/aws.py | head -60
  </verify>
  <done>
    wait_for_started() implementation exists with:
    - Exponential backoff (backoff * 2^attempt, capped at 60s)
    - Polls until status == InstanceState.RUNNING
    - Handles None from get_instance() (NotFound) with retry
    - Raises TimeoutError if limit exceeded
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for wait_for_started()</name>
  <files>tests/cloud_providers/test_aws.py</files>
  <action>
    Add comprehensive unit tests for wait_for_started() method.

    Tests to add:
    1. test_wait_for_started_success: Mock instance transitioning from pending to running, verify returns when RUNNING
    2. test_wait_for_started_already_running: Mock instance already running, verify immediate return
    3. test_wait_for_started_timeout: Mock instance stuck in pending, verify TimeoutError raised
    4. test_wait_for_started_not_found_retry: Mock get_instance returning None initially, then valid instance
    5. test_wait_for_started_exponential_backoff: Verify sleep intervals increase exponentially (backoff=1 for faster test)

    Use moto @mock_aws decorator pattern from existing tests.
    For testing backoff without long waits, use backoff=0 or very small values.

    Test pattern for state transitions with moto:
    - Create instance (starts as pending in moto)
    - Manually update instance state via moto's internal mechanisms if needed
    - Or mock get_instance to return different CloudInstance objects on each call

    For exponential backoff testing:
    - Use AsyncMock with side_effect to track sleep calls
    - Verify sleep durations follow exponential pattern
  </action>
  <verify>
    python -m pytest tests/cloud_providers/test_aws.py::test_wait_for_started_success tests/cloud_providers/test_aws.py::test_wait_for_started_timeout -v 2>&1 | head -30
  </verify>
  <done>
    All wait_for_started tests pass:
    - test_wait_for_started_success
    - test_wait_for_started_already_running
    - test_wait_for_started_timeout
    - test_wait_for_started_not_found_retry
    - test_wait_for_started_exponential_backoff
  </done>
</task>

</tasks>

<verification>
1. wait_for_started() implementation complete in aws.py
2. All 5 unit tests pass with moto mocks
3. Exponential backoff verified (sleep durations increase)
4. InvalidInstanceID.NotFound retry logic tested
5. TimeoutError raised correctly when limit exceeded
</verification>

<success_criteria>
- wait_for_started() polls get_instance() until status == InstanceState.RUNNING
- Exponential backoff used (backoff doubles each attempt, capped at 60s)
- InvalidInstanceID.NotFound handled by continuing to poll (treat as eventual consistency)
- TimeoutError raised with descriptive message after limit attempts
- 5 unit tests covering success, timeout, retry, backoff, and edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/04-instance-lifecycle-waiting/04-01-SUMMARY.md`
</output>
