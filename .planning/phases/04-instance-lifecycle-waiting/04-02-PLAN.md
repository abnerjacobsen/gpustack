---
phase: 04-instance-lifecycle-waiting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gpustack/cloud_providers/aws.py
  - tests/cloud_providers/test_aws.py
autonomous: true
must_haves:
  truths:
    - System polls for public IP assignment until IP is available
    - wait_for_public_ip() returns CloudInstance when ip_address is set
    - wait_for_public_ip() raises TimeoutError if limit exceeded
    - wait_for_public_ip() handles InvalidInstanceID.NotFound with retry logic
    - Polling uses exponential backoff (not fixed interval)
  artifacts:
    - path: gpustack/cloud_providers/aws.py
      provides: wait_for_public_ip() method
      exports: ["wait_for_public_ip"]
      min_lines: 30
    - path: tests/cloud_providers/test_aws.py
      provides: Test coverage for wait_for_public_ip()
      contains: ["test_wait_for_public_ip_success", "test_wait_for_public_ip_timeout", "test_wait_for_public_ip_not_found_retry"]
  key_links:
    - from: wait_for_public_ip()
      to: get_instance()
      via: instance = await self.get_instance(external_id)
      pattern: "get_instance.*external_id"
    - from: wait_for_public_ip()
      to: CloudInstance.ip_address
      via: instance.ip_address is not None
      pattern: "instance.ip_address"
---

<objective>
Implement wait_for_public_ip() method to poll EC2 instance until it receives a public IP address.

Purpose: GPUStack workers need a public IP to connect back to the server. This method waits for IP assignment with exponential backoff, handling AWS's eventual consistency.
Output: Working wait_for_public_ip() method with comprehensive tests.
</objective>

<execution_context>
@/home/abner/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/abner/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@gpustack/cloud_providers/aws.py
@gpustack/cloud_providers/digital_ocean.py
@tests/cloud_providers/test_aws.py
@gpustack/cloud_providers/abstract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement wait_for_public_ip() method</name>
  <files>gpustack/cloud_providers/aws.py</files>
  <action>
    Replace the NotImplementedError stub for wait_for_public_ip() with full implementation.

    Implementation requirements:
    1. Poll get_instance() in a loop until instance.ip_address is not None
    2. Use exponential backoff: sleep_time = backoff * (2 ** attempt) - capped at 60s max
    3. Handle InvalidInstanceID.NotFound by continuing to retry (AWS eventual consistency)
    4. Raise TimeoutError with descriptive message if limit exceeded
    5. Return CloudInstance once IP is assigned

    Key details:
    - Keep the method signature: async def wait_for_public_ip(self, external_id: str, backoff: int = 15, limit: int = 20)
    - Log each poll attempt at DEBUG level: logger.debug(f"Waiting for public IP for instance {external_id}, attempt {attempt}/{limit}, IP: {ip}")
    - Check for both None and empty string: if instance.ip_address is not None and instance.ip_address != ""
    - After loop, check if instance is None and raise RuntimeError (not expected after NotFound handling)

    Exponential backoff calculation:
    ```python
    sleep_time = min(backoff * (2 ** attempt), 60)  # Cap at 60 seconds
    ```

    InvalidInstanceID.NotFound handling:
    - When get_instance() returns None (from NotFound error), treat as instance not yet visible
    - Continue polling - AWS eventual consistency means instance may not appear immediately
    - Log at DEBUG: "Instance {external_id} not yet visible, retrying..."

    Default timeout calculation:
    - With backoff=15 and limit=20, max wait is approximately 15+30+60+60*17 = ~17 minutes
    - This is sufficient for AWS public IP assignment which typically takes 1-3 minutes
  </action>
  <verify>
    grep -A 50 "async def wait_for_public_ip" gpustack/cloud_providers/aws.py | head -60
  </verify>
  <done>
    wait_for_public_ip() implementation exists with:
    - Exponential backoff (backoff * 2^attempt, capped at 60s)
    - Polls until instance.ip_address is not None and not empty
    - Handles None from get_instance() (NotFound) with retry
    - Raises TimeoutError if limit exceeded
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for wait_for_public_ip()</name>
  <files>tests/cloud_providers/test_aws.py</files>
  <action>
    Add comprehensive unit tests for wait_for_public_ip() method.

    Tests to add:
    1. test_wait_for_public_ip_success: Mock instance transitioning from no IP to having IP, verify returns when IP available
    2. test_wait_for_public_ip_already_has_ip: Mock instance already has IP, verify immediate return
    3. test_wait_for_public_ip_timeout: Mock instance stuck without IP, verify TimeoutError raised
    4. test_wait_for_public_ip_not_found_retry: Mock get_instance returning None initially, then valid instance with IP
    5. test_wait_for_public_ip_empty_string: Mock instance with ip_address="", verify continues polling
    6. test_wait_for_public_ip_exponential_backoff: Verify sleep intervals increase exponentially

    Use moto @mock_aws decorator pattern from existing tests.
    For testing backoff without long waits, use backoff=0 or very small values.

    Test pattern for IP assignment with moto:
    - Create instance with network interface and AssociatePublicIpAddress=True
    - Use moto's internal mechanisms to simulate IP assignment, OR
    - Mock get_instance to return CloudInstance objects with varying ip_address values

    For testing empty string handling:
    - Create mock returning ip_address="" initially, then real IP
    - Verify method doesn't return on empty string
  </action>
  <verify>
    python -m pytest tests/cloud_providers/test_aws.py -k "wait_for_public_ip" -v 2>&1 | head -40
  </verify>
  <done>
    All wait_for_public_ip tests pass:
    - test_wait_for_public_ip_success
    - test_wait_for_public_ip_already_has_ip
    - test_wait_for_public_ip_timeout
    - test_wait_for_public_ip_not_found_retry
    - test_wait_for_public_ip_empty_string
    - test_wait_for_public_ip_exponential_backoff
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite for Phase 4</name>
  <files>tests/cloud_providers/test_aws.py</files>
  <action>
    Run the complete AWS test suite to ensure all tests pass together.

    Execute:
    ```bash
    python -m pytest tests/cloud_providers/test_aws.py -v --tb=short
    ```

    Verify:
    - All existing tests still pass (credential validation, SSH keys, instances)
    - All new wait_for tests pass
    - No regressions introduced
    - Test count increased by 11 (5 for wait_for_started + 6 for wait_for_public_ip)

    Check test coverage:
    ```bash
    python -m pytest tests/cloud_providers/test_aws.py --cov=gpustack.cloud_providers.aws --cov-report=term-missing 2>&1 | tail -30
    ```

    Expected coverage improvements:
    - wait_for_started() fully covered
    - wait_for_public_ip() fully covered
    - Exponential backoff logic covered
    - Error handling paths covered
  </action>
  <verify>
    python -m pytest tests/cloud_providers/test_aws.py -v 2>&1 | tail -20
  </verify>
  <done>
    Full test suite passes with all wait_for tests included
    Original 26 tests + 11 new tests = 37 tests passing
  </done>
</task>

</tasks>

<verification>
1. wait_for_public_ip() implementation complete in aws.py
2. All 6 unit tests pass with moto mocks
3. Exponential backoff verified (sleep durations increase)
4. InvalidInstanceID.NotFound retry logic tested
5. Empty string IP handling tested
6. Full test suite passes without regressions
</verification>

<success_criteria>
- wait_for_public_ip() polls get_instance() until ip_address is not None and not empty
- Exponential backoff used (backoff doubles each attempt, capped at 60s)
- InvalidInstanceID.NotFound handled by continuing to poll (treat as eventual consistency)
- TimeoutError raised with descriptive message after limit attempts
- 6 unit tests covering success, timeout, retry, backoff, empty string, and edge cases
- Full test suite (37 tests) passes without regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-instance-lifecycle-waiting/04-02-SUMMARY.md`
</output>
